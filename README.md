# Hyperledger Fabric File/Folder Storage
A permissioned blockchain (Hyperledger Fabric) to store file metadata in a folder hierarchy. Files’ content is stored off-chain (e.g. IPFS or object store), while the ledger records metadata (filenames, paths, permissions, and content hashes).
